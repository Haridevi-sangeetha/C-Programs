#include<stdio.h>
#include<math.h>

int main(){
    int number;
    scanf("%d",&number);
    int temp1 = number;
    int digit = 0;
    while(number != 0){
        int temp = number%10;
        digit +=1;
        number=number/10;
    }
    int power = pow(10,(digit-1));
    int last = temp1%10;
    int first  = temp1/power;
    int middle = (temp1%power)/10;
    
    int result = last*power+middle*10+first;
    printf("%d",result);
    return 0;
}