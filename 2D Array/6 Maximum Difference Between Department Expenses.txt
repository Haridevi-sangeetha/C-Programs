


#include<stdio.h>

int main(){
    int row,col;
    scanf("%d %d",&row,&col);
    int arr[row][col];
   
    for(int i=0;i<row;i++){
        for(int j=0;j<col;j++){
            scanf("%d",&arr[i][j]);
        }
    }
    int sum ;
    int max_sum = 0;
    for(int i=0;i<row;i++){
        sum = 0;
        for(int j=0;j<col;j++){
            sum = sum+arr[i][j];
        }
        if(sum>max_sum){
            max_sum = sum;
        }
    }
    // printf("%d",max_sum);
    int sum2 ;
    int difference ;
    for(int i=0;i<row;i++){
        for(int j=0;j<col;j++){
            sum =sum+arr[i][j];
        }
        difference = sum - max_sum;
    }
    printf("Maximum Difference : %d",difference);
    return 0;
}