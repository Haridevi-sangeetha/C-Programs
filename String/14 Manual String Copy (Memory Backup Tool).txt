#include<stdio.h>
#include<string.h>

int main(){
    char str[100];
    fgets(str,sizeof(str),stdin);
    char str2[100];
    int len = strlen(str);
    for(int i=0;str[i]!='\0';i++){
        str2[i] = str[i];
    }
    printf("%s",str);
    return 0;
}