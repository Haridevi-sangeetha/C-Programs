#include<stdio.h>
int result(){
    int p,r,t;
    scanf("%d %d %d",&p,&r,&t);
    float interst = (p*r*t)/100;
    printf("%0.2f",interst);
    return 0;
}
int  main(){
    result();
    return 0;
}
