#include<stdio.h>

int  Mul_table(){
    int num;
    scanf("%d",&num);
    for(int i=1;i<=10;i++){
        printf("%d X %d = %d\n",num,i,i*num);
    }
    return 0;
}
int main(){
    Mul_table();
    return 0;
}