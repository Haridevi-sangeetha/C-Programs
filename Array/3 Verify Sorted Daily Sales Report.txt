
#include<stdio.h>

int main(){
    int size;
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<size;i++){
        scanf("%d",&arr[i]);
    }
    int count = 0;
    int first = arr[0];
    // printf("First : %d\n",first);
    int sum =0;
    for(int i=0;i<size;i++){
        if(arr[i]<=arr[i+1]){
            count+=1;
        }
    }
    if(count == size){
        printf("YES");
    }
    else{
        printf("NO");
    }
    return 0;
}