#include<stdio.h>

int main(){
   int age;
   scanf("%d",&age);
   if(age>=18){
    printf("%d is Eligible to Vote.",age);
   }
   else{
    printf("%d is Not Eligible.",age);
   }
   return 0;
}