

#include<stdio.h>

int main(){

/* 1.Printing statements */

    //1. Print Hello World
    printf("Hello World\n");
    // 2. Print your name
    char name[100];
    scanf("%s",name);
    printf("%s is my name.\n",name);
    // 3.Print an integer value
    int a;
    scanf("%d",&a);
    printf("%d\n",a);
    // 4.Print a floating-point number
    float num;
    scanf("%f",&num);
    printf("%f\n",num);
    // 5. Print a character
    char ch;
    scanf(" %c",&ch);
    printf("%c\n",ch);
    // 6. Print two integers in one line
    int c,d;
    scanf("%d %d",&c,&d);
    printf("%d %d\n",c,d);
    // 7.Print a string along with a number
    int age ;
    scanf("%d",&age);
    printf("Age : %d\n",age);
    // // 8.Print output in two lines using \n
    char word[100];
    char word2[100];
    scanf("%s %s",word,word2);
    printf("%s\n%s",word,word2);
   ----------------------------------------------------------------------------------------------------------------

/* 2.Variables in c */

    // 1. Declare an integer variable and print it
    int a=10;
    printf("%d\n",a);
    // 2.Declare two integer variables and print them
    int b=10,c=19;
    printf("%d %d\n",b,c);
    // 3. Declare a float variable and print it
    float d,e;
    scanf("%f %f",&d,&e);
    printf("%.1f\n%.2f\n",d,e);
    // 4. Declare a character variable and print it
    int x = 10;
    printf("%d\n",x);
    x = 20;
    printf("%d\n",x);
    // 5.Change variable value and print updated value
    int num;
    scanf("%d",&num);
    num = 10;
    printf("%d\n",num);
    // 6.Add two variables
    int f,g;
    scanf("%d %d",&f,&g);
    int add = f+g;
    printf("%d\n",add);
    // 7. Subtract two variables
    int h,i;
    scanf("%d %d",&h,&i);
    int sub = h-i;
    printf("%d\n",sub);
    // 8. Multiply two variables
    int j,k;
    scanf("%d %d",&j,&k);
    int mul = j*k;
    printf("%d\n",mul);
    // 9.Divide two variables
    int l ,m;
    scanf("%d %d",&l,&m);
    int div = l/m;
    printf("%d\n",div);
    // 10.Assign value later and print
    int n=10;
    printf("%d",n);
  --------------------------------------------------------------------------------------------------------------------------
    /* 3.Data Types in c */


    // 1. Print int variable
    int num;
    scanf("%d",&num);
    printf("%d",num);
    //2.Print float variable
    float val;
    scanf("%f",&val);
    printf("%f",val);
    //3.Print double variable
    double num1;
    scanf("%lf",&num1);
    printf("%lf",num1);
    // 4.Print char variable
    char ch;
    scanf("%c",&ch);
    printf("%c",ch);
    // 5.Size of int
    int a;
    printf("%d",sizeof(a));
    // 6.Size of float
    float b;
    printf("%d",sizeof(b));
    // 7.Size of double
    double d;
    printf("%d",sizeof(d));
    // 8.Size of char
    char c;
    printf("%d",sizeof(c));
    // 9.Assign int to float
    int sum ;
    scanf("%d",&sum);
    float f = sum;
    printf("%.2f",f);
    // 10.Store char in int
    char ch2;
    scanf("%c",&ch2);
    printf("%d",ch2);
    // 11. Print the sum of two integers using +.
    int e,f;
    scanf("%d %d",&e,&f);
    int add = e+f;
    printf("%d",add);
    // 12.Print the difference of two integers using -.
    int g,h;
    scanf("%d %d",&g,&h);
    int diff = g-h;
    printf("%d",diff);
    // 13. Print the product of two integers using *.
    int i,j;
    scanf("%d %d",&i,&j);
    int mul = i*j;
    printf("%d",mul);
    // 14. Print quotient and remainder of two integers.
    int k,l;
    scanf("%d %d",&k,&l);
    int quo = k/l;
    int rem = k%l;
    printf("Quotient is %d\n",quo);
    printf("Reminder is %d",rem);
    // 15.Add three integers and print the result.
    int m,n,o;
    scanf("%d %d %d",&m,&n,&o);
    int sum1 = m+n+o;
    printf("%d",sum1);

 ------------------------------------------------------------------------------------------------------------------------------------------------

 /* Increment and Decrement operators */

    // 1.Apply pre-increment to a variable.
    int a;
    scanf("%d",&a);
    printf("%d",(++a));
    // 2.Apply post-increment and print the value.
    int b;
    scanf("%d",&b);
    printf("%d",(b++));
    // 3.Apply pre-decrement to a variable.
    int c;
    scanf("%d",&c);
    printf("%d",(--c));
    printf("%d",(c--));
    // 4.Increment a variable twice and print result.
    int d;
    scanf("%d",&d);
    printf("%d",(d+2));
    // 5.Decrement a variable twice and print result.
    int e;
    scanf("%d",&e);
    printf("%d",(e-2));

-----------------------------------------------------------------------------------------------------------------------------------------------------------

  /* Relational operators */

    / 1.
    int a,b;
    scanf("%d %d",&a,&b);
    // 1. Compare two numbers using >.
    printf("%d\n",(a>b));
    // 2. Compare two numbers using <.
    printf("%d\n",(a<b));
    // 3. Compare two numbers using ==.
    printf("%d\n",(a==b));
    // 4. Compare two numbers using !=.
    printf("%d\n",(a!=b));
    // 5. Compare two numbers using >= and <=.
    printf("%d %d",(a>=b),(a<=b));

----------------------------------------------------------------------------------------------------------------------------------------

 /*  Logical Operators (&& || !) */

    int a,b;
    scanf("%d %d",&a,&b);
    // 1. Use logical AND on two non-zero values.
    printf("%d\n",(a&&b));
    // 2. Use logical OR on two values.
    printf("%d\n",(a||b));
    // 3. Apply logical NOT on zero.
    printf("%d\n",(!a));
    // 4. Check two conditions using AND and OR.
    printf("%d",((a&&b)&&(a||b)));

--------------------------------------------------------------------------------------------------------------------------------------------------------------

/*  Assignment Operators (= += -= *= /= %=) */
    
    // 1. Assign a value and print it.
    int c;
    scanf("%d",&c);
    printf("%d\n",c);
    int a,b;
    scanf("%d %d",&a,&b);
    // 2. Use += operator.
    a +=b;
    printf("%d\n",a);
    // 3. Use -= operator.
    a -=b;
    printf("%d\n",a);
    // 4. Use *= operator.
    a *= b;
    printf("%d\n",a);
    // 5. Use /= and %= operators.
    a /=b;
    printf("%d\n",a);
    a %=b;
    printf("%d\n",a);
----------------------------------------------------------------------------------------------------------------------------------------------------------------
}