#include<stdio.h>
#include<limits.h>

int main(){
    int size;
    scanf("%d",&size);
    int arr[size];
    for(int i=0;i<size;i++){
        scanf("%d",&arr[i]);
    }
    int *p = arr;
    int largest = INT_MIN;
    int *n1 = &largest;
    for(int i=0;i<size;i++){
        if(*(p+i)>*n1){
            *n1 = *(p+i);
        // printf("%d",arr[i]);
        }
    }
    printf("%d",*n1);
    return 0;
}