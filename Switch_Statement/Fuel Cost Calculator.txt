#include <stdio.h>

int main() {
    int fuelType, litres;
    int costPerLitre = 0;
    int totalCost;


    scanf("%d", &fuelType);
    scanf("%d", &litres);

    switch (fuelType) {
        case 1:   
            costPerLitre = 105;
            break;

        case 2:  
            costPerLitre = 92;
            break;

        case 3:   
            costPerLitre = 85;
            break;

        default:
            printf("Invalid fuel type");
    }

    totalCost = costPerLitre * litres;

    printf("Fuel Cost : %d", totalCost);

    return 0;
}