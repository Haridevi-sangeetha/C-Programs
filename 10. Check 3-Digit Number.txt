#include<stdio.h>

int main(){
   int num;
   scanf("%d",&num);
   if(num>=100 && num<=999){
    printf("%d is Three - digit Number.",num);
   }
   else{
    printf("Not Three-digit number.");
   }
   return 0;
}